<!-- DECISIONS.md — every design decision backed by a number (constitution Art. V).
     One row per decision: what, the number that justifies it, and the date. -->

# Raseed Decisions Log

Every design decision is recorded here with the number that backs it. Phases append
rows; nothing is decided "by feel."

| Date | Decision | Number / Rationale | Phase |
|------|----------|--------------------|-------|
| 2026-06-12 | Trainer is the single heavy image, off the default compose profile | 0 serving images contain torch (Art. III) | 0 |
| 2026-06-12 | Pinned runtimes: Python 3.12, Node 20, Postgres 16, Redis 7 | Reproducible fresh-clone boot (SC-001) | 0 |
| 2026-06-12 | CI runs lint + type-check only this phase | Brief/PLAN scope; full 8 gates land per phase | 0 |
| 2026-06-14 | memory.embedding deferred to Phase 4 with the embedder decision | embedder/dimension chosen in Phase 4 (DESIGN F) and `write_memory` doesn't exist until then; the column would be unwritable dead schema now | 1 |
| 2026-06-14 | RLS reset strategy: session-dep finally-block + pool event hook (defense in depth) | R1: session GUC persists on pooled connection; explicit RESET on both connection teardown paths ensures SC-003 (no identity bleed) | 1 |
| 2026-06-14 | JWT signing secret stored in Settings; Vault overrides at startup in non-local envs; APP_ENV=local uses .env default | R5: fail-fast refuse-to-boot on missing Vault secret in production; local trim rung documented in .env.example (Art. V) | 1 |
| 2026-06-14 | Retry policy: max 3 attempts, exponential backoff 1-8s, 30s timeout, 4xx not retried | R6/Art. V: bounded backoff prevents quota exhaustion; 4xx is a client error — retrying wastes budget and won't fix it | 1 |
| 2026-06-14 | deps.py holds auth concern only; RLS-scoped session dep moved to db/session.py | M2: separates auth from persistence concern; eliminates parallel-task conflict between T014 and T019 | 1 |
| 2026-06-15 | Categorizer serves from a mounted, SHA-256-pinned LFS artifact; artifact-store (MinIO) loading + hot reload deferred to Phase 5, behind a thin "get current artifact" seam | Trade-off: the foundation artifact is immutable this phase — nothing to rotate — so a mounted, content-pinned artifact is the minimal surface satisfying refuse-to-boot (Art. III) without inventing runtime artifact rotation that doesn't exist yet. The seam keeps boot/hash-verify logic source-agnostic so Phase 5 swaps mount→store-by-SHA without touching it. MinIO stays "model artifacts only" (untouched). Reconciles spec FR-016 with plan R7. | 2 |
| 2026-06-15 | Sparse-class operating threshold: a category with < 20 validation samples → always_review (never auto-accepted) | 20 = minimum sample count for a per-class precision estimate to mean anything at the 97% target; below it a validated threshold cannot be established, so fail safe to review. Revisit only if prep shows the taxonomy far smaller than expected. | 2 |

| 2026-06-15 | ~~Phase 2 champion = TF-IDF+LR baseline; DistilBERT deferred pending real data~~ **(SUPERSEDED 2026-06-16)** | Original Kaggle set (Banking_Transactions_USA_2023_2024.csv) had synthetic merchant names → no learnable signal (macro-F1 ≈ 0.07–0.09). Replaced with a real dataset on 2026-06-16; see rows below. | 2 |
| 2026-06-15 | ~~macro_f1_min gate floor = 0.04~~ **(SUPERSEDED 2026-06-16: now 0.84)** | Was seeded from the synthetic-data holdout (0.0724). Re-seeded from the real-data champion below. | 2 |
| 2026-06-15 | ONNX export uses base_pipeline (TF-IDF+LR Pipeline), not CalibratedClassifierCV wrapper | skl2onnx cannot convert CalibratedClassifierCV with StringTensorType input (calibrator expects float features); export the inner pipeline which handles string->proba natively. Calibrator trained with cv="prefit" on val split is used for inference in Python context only. | 2 |
| 2026-06-16 | **Dataset switched** to `laramee26openBankTransactionData.xlsx` (UK/GBP open banking); old synthetic Kaggle CSV and its derived files deleted | The real dataset has genuine merchant-name signal. Same TF-IDF+LR pipeline jumps from macro-F1 0.07 → **0.8934 holdout / 0.8361 val**. Pipeline, gate, and model-server code unchanged — only data + taxonomy. | 2 |
| 2026-06-16 | Null-Category handling: dropped **24** rows (the real count in this 6,567-row file) | The dataset research cited "1,771 (21%)" nulls; that figure is from a differently-sized copy of the file. We drop the rows that are actually null here (24) and record the discrepancy rather than inventing a number. | 2 |
| 2026-06-16 | `SAVE THE CHANGE` down-sampled **1,165 → 150** (seed=7) | It is one identical string repeated 1,165× (~18% of labeled data); left intact it inflates the `savings` class and overall accuracy. Cap of 150 leaves `savings` at 236, mid-pack with the other major classes. Evaluation reports per-class F1, not just macro. | 2 |
| 2026-06-16 | Cleaned, **locked taxonomy = 18 categories** (v2.0.0); target was ~15 | Dropped 6 singletons (Rent, Health, Account transfer, Purchase of uk.eg.org, Safety Deposit Return, Travel Reimbursement); merged Food Shopping→groceries, Paycheck+Supplementary Income→income, Investment+Interest→investment, Home Improvement+Services/Home Improvement→home_improvement. Landed at 18 (not 15) to preserve the explicitly-named thin watch-classes (fitness/clothes/hotels) and keep financially-distinct mortgage/insurance separate from bills. `amazon` kept as its own class (dataset quirk). `other` is a filter sentinel, not a model label. | 2 |
| 2026-06-16 | Input text = `Transaction Description` + bracketed `Transaction Type` code (e.g. `LIDL GB NOTTINGHA [DEB]`) | Type codes (DEB/BP/DD/CPT/FPI/BGC…) are weak but real signal (DD≈recurring bills, FPI/BGC≈income inflows). Appended as a token so the model can use it without it swamping the description. | 2 |
| 2026-06-16 | Real-data champion = TF-IDF+LR (DistilBERT still pending); floor `macro_f1_min` = **0.84** (holdout 0.8934 − 0.05 tolerance), `beat_baseline_margin` = 0.0 | Baseline already clears a strong bar; it ships as champion until the Colab DistilBERT run beats it. 0.05 tolerance survives split reshuffles while still catching real regressions. | 2 |
| 2026-06-16 | `min_per_class_f1` gate stays **0.0**; thin classes governed by always_review | The thin classes have <20 val samples (fitness F1=0.0, clothes=0.44 on val); a per-class F1 floor would gate on noise. The N<20 → always_review rule already fails them safe at serve time, so the gate does not double-penalize. 9 of 18 classes carry a real ≥97%-precision threshold; 9 are always_review. | 2 |
| 2026-06-16 | Data scope recorded as **UK/GBP** in the model card | Merchants, type codes, and amounts are British; a US/other-locale deployment needs re-training on local data. Documented so the limitation is explicit, not discovered in production. | 2 |
| 2026-06-16 | DistilBERT fine-tune **ran and lost** (holdout 0.8677 < baseline 0.8934); baseline confirmed champion | Local 3-epoch CPU fine-tune via `train_champion_local.py` (faithful recipe of the Colab notebook: full fine-tune + temperature scaling on val). On short high-signal merchant strings, n-gram TF-IDF beats DistilBERT at this epoch/compute budget. Per the recorded winner rule, the weaker model is not promoted (the gate would block it anyway). A 5-epoch GPU Colab run may close the gap but was not needed to clear the gate. This completes the genuine three-way comparison (SC-006). | 2 |

| 2026-06-17 | One `ingest_transactions()` function, three entry points (upload, form, future agent tool) | R1: a single path means one place for PAN/IBAN scrubbing, the confidence gate, and provenance tagging — no divergence between entry points (constitution Art. II/III) | 3 |
| 2026-06-17 | Raw statement bytes are never persisted; parse in-memory, return ParsedRow, discard bytes | R2/constitution Art. II: CSV bytes that might contain PAN/IBAN numbers must not touch any persistent store; the parser scrubs before returning rows | 3 |
| 2026-06-17 | Confidence gate reuses Phase-2 per-category `operating_thresholds` from `eval_thresholds.yaml`; `always_review` categories always set `needs_review=true` | R3: reuses calibrated precision-targeted thresholds already validated in Phase 2 rather than inventing a second threshold; avoids dual-maintenance | 3 |
| 2026-06-17 | Dedup natural key = `(user_id, occurred_at, amount, normalized_description)` — ON CONFLICT DO NOTHING | R8: re-uploading overlapping statements must not create duplicate history; normalization = lowercase+trim; duplicate-charge anomaly uses a separate time-window rule to distinguish legitimate repeat purchases | 3 |
| 2026-06-17 | Recompute triggered on every write (invalidate-on-write); reads are plain DB reads of stored derived rows | R7/constitution Art. V: derived data must never be stale longer than worker latency; time-based expiry is explicitly forbidden for transaction-derived data | 3 |
| 2026-06-17 | Cold-start threshold = 30 days; users below threshold get a day-of-week average projection, `is_cold_start=true` | R5: 30 days agreed in clarification 2026-06-16; below 30 days there is insufficient history for Prophet to produce meaningful uncertainty intervals | 3 |
| 2026-06-17 | Prophet runs in the light worker only (`workers/recompute.py`), never in the lean model-server image | R4/constitution Art. III: no torch/heavy deps in serving images; Prophet (scipy, pystan) is only imported in the worker container | 3 |
| 2026-06-17 | `population_stats` is written only by the privileged `raseed_stats` BYPASSRLS job; user sessions (raseed_app) have SELECT only | R5/constitution Art. II: only a privileged job may aggregate cross-user data; k-anonymity guard suppresses categories with < 5 distinct users | 3 |
| 2026-06-17 | Anomaly detection: robust IQR (Q1/Q3 ± 2.5 × IQR) per category (min 5 samples) + duplicate-charge rule (same merchant+amount within 2 days) | R6: IQR is robust to the outliers being sought; mean/stddev z-score is distorted by them; duplicate rule catches double-charges the statistical rule misses | 3 |
| 2026-06-17 | Recurring detector groups by merchant, requires ≥ 3 occurrences, maps avg inter-charge delta to nearest standard cadence (7/14/30/90/365 days, tolerance ±5 days) | R6: minimum 3 occurrences is the minimum to establish a pattern; irregular cadence (>5-day tolerance from all standard intervals) is not reported as a subscription | 3 |

| 2026-06-17 | Category correction action deferred to Phase 5; category badge is read-only this phase | No `/corrections` endpoint exists; `docs/PLAN.md` assigns the corrections store and review queue to the ML-lifecycle phase. Adding a write path without the store would create dead UI — the badge stays read-only and the clarification is recorded in `specs/005-dashboard-ui/spec.md`. | 3b |
| 2026-06-17 | Frontend test tooling = Vitest + React Testing Library (jsdom) | Stack-independent (constitution Art. V): tests run without starting the Docker Compose stack. Vitest shares Vite's transform pipeline (no separate jest config); RTL aligns with React 18 idioms. | 3b |
| 2026-06-17 | Dashboard fetches all data in a single `GET /dashboard` call; user triggers Refresh to re-poll after recompute | Research R6: recompute worker runs seconds after upload — polling vs Refresh. A manual Refresh button gives users control without constant network traffic; eventual-consistency is surfaced by the "Dashboard will refresh shortly" banner on upload. No per-panel granularity this phase. | 3b |

| 2026-06-17 | Agent loop cap = 8 iterations / ~16k tokens per turn | FR-006: bounds latency and cost. 8 iterations is enough for affordability (4 reads + knowledge + analysis + synthesis + one retry). 16k tokens ≈ 2× the typical context needed; forced synthesis on cap prevents infinite loops. | 4 |
| 2026-06-17 | Session memory TTL = 30-minute sliding idle window (Redis, ephemeral only) | FR-017/clarification Q1: ephemeral (not persisted); 30 min covers a realistic chat session without accumulating cross-session state. Sliding refresh means an active conversation never expires mid-flow. | 4 |
| 2026-06-17 | Write rate limit = 10 writes/min/user (Redis fixed-window counter) | FR-020: prevents LLM-triggered write loops from writing unbounded transactions or memories. 10/min is generous for human-paced chat while blocking any automated abuse. | 4 |
| 2026-06-17 | Embedding = Gemini `models/text-embedding-004`, 768 dimensions, reuses `gemini_api_key` (no separate embedding key) | No separate key simplifies Vault secrets. Gemini text-embedding-004 is stable at 768 dim; pgvector ivfflat with lists=10 is correct for corpus scale (dozens of documents → low thousands of passages). FakeEmbedder (hash-seeded) used in CI — no hosted API call. | 4 |
| 2026-06-17 | JSON-action ReAct loop: LLM outputs `{"tool": ..., "args": ...}` or `{"final": ..., "citations": [...]}` (research R1) | Provider-agnostic (works with FakeLLM, Gemini, Grok) via existing `infra/llm.py` `complete()`. Native function-calling APIs would lock to one provider. JSON protocol is inspectable and testable. | 4 |
| 2026-06-17 | Hybrid retrieval = pgvector cosine (dense) + Postgres FTS ts_rank (sparse), fused via RRF (k=60) with no-answer floor = 0.01 (research R4/R5) | RRF is parameter-free and robust to scale differences between dense scores (0–1) and FTS scores (0–∞). No-answer floor of 0.01 rejects off-corpus questions without needing a calibrated threshold per domain. | 4 |
| 2026-06-17 | Gate 3 (tool-selection) floor = 0.80 (12/15 cases); Gate 4 (RAG) floors = 0.0 for FakeEmbedder CI run | Gate 3: 15-case golden set measured with FakeLLM+FakeEmbedder — deterministic routing accounts for 6/15 cases; agent routing the rest. 0.80 catches regressions in pattern matching. Gate 4: FakeEmbedder is hash-based (non-semantic), so hit@5/MRR are non-deterministic at 0; floor will be raised after first real-embedder run (see PLAN.md). | 4 |
| 2026-06-17 | RAG metadata-filter, re-ranking, and query-rewrite: not wired this phase (cut, FR-015) | Corpus is small (low-thousands of passages); RRF+dense+sparse already sufficient at this scale. Re-ranking adds latency and a hosted-model call with minimal quality gain. Query rewrite prompt created but not wired; add if MRR on the real-embedder run is <0.7. | 4 |
| 2026-06-17 | Rails (input/output check + redact) are no-op call sites this phase; Phase 6 fills them | FR-022: call sites exist in the chat path so Phase 6 wires safety checks without re-plumbing the entire request pipeline. No behavior this phase. | 4 |

<!-- Later phases: retrain trigger (100 corrections OR 14 days), drift signals, etc. -->
