<!-- Colab foundation-training notebooks live here: the initial DistilBERT
     fine-tuning runs offline on GPU in Colab, exports ONNX + model card + SHA.
     In-stack retrains (partial-unfreeze, CPU) live in the trainer service, not
     here (constitution Art. III). Placeholder in Phase 0. -->

# Training notebooks

Offline foundation-training notebooks (Colab, GPU). Added in Phase 2. The exported
ONNX artifact, model card, and pinned SHA-256 are what the lean model-server loads.
